server:
  port: 8811
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: springclouddemoconfigserver
#      uri: http://localhost:8801/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
